version: 2.1
orbs:
  browser-tools: circleci/browser-tools@1.2

commands:
  run-tests:
    steps:
      - run:
          name: Run Tests
          command: ./tests-php-lightning-services.sh

executors:
  docker-executor:
    docker: # run the steps with Docker
      - image: cimg/base:2022.08

jobs:
  test-php-with-docker:
    executor: docker-executor
    steps:
      - checkout
      - run-tests

workflows:
  test-with-docker:
    jobs:
      - test-php-with-docker
